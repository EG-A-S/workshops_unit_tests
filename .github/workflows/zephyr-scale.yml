name: Zephyr Scale

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
    
jobs:
  zephyrscale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Checkout code  
        uses: EG-A-S/cto-actions-zephyr@v0.1
        with:
            tests_kind: 'JUnit'
            tests_path: './src/test/java/com/example/javamavenjunithelloworld/'
            jira_project: 'DEM'      
            auto_test_creation: 'True'
            api_token: ${{secrets.JIRA_TOKEN}}